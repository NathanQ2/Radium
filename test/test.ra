// This is a comment!
// Define entrypoint
func main() {
    // Create 'x' on the stack
    let x = 10 + 5;
    
    // Call function
    myFunc();
       
    // Exit with return code 'x'
    exit(x);
    ret 0;
}

func myFunc() {
    ret 5;
}
