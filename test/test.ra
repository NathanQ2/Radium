// This is a comment!
// Define entrypoint
func main() {
    let x = 5;
    
    //let y = myFunc();
    
    if (x - 6) {
        // if (10 - 1) {
        //     exit(10);
        // };
        exit(5);
    };
    
    exit(0);
    //ret y;
}

func myFunc() {
    ret 15 + 15;
}
