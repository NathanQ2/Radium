// This is a comment!
// Define entrypoint
func main() {
    let x = 1;
    
    //let y = myFunc();
    
    if (x) {
        if (0) {
            exit(10);
        };
        exit(5);
    };
    
    exit(0);
    //ret y;
}

func myFunc() {
    ret 15 + 15;
}
